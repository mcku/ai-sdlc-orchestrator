# Phase 05 — QA

Verify the change does what it claims, doesn't break anything else, meets the coverage gate, and passes the project's pre-commit hooks.

## Inputs

- `sessions/<id>/03-planning.md` (test strategy)
- Repo state after phase 04

## Outputs

- `sessions/<id>/05-qa.md` containing:
  - Tests added/updated and what they cover
  - Test run results (pass/fail, with output excerpts)
  - **Coverage gate result** (diff coverage %, tool used, any approved exclusions)
  - Regression checks performed
  - UI test results (if applicable)
  - **Pre-commit gate result** (hooks run, outcome, any auto-fixes applied)
  - Known gaps / things not covered

## Gates (in order)

Each gate's `enabled` value comes from the project's `.ai-sdlc.yaml` (`gates.<name>`); defaults are all `true`. See `config.example.yaml` for the schema. A gate set to `false` is skipped entirely; a gate set to `manual` runs only on explicit user request.

1. **Tests pass.** Unit tests, plus the agreed regression subset. Always required (not configurable).
2. **Coverage gate** — `playbooks/coverage-gate.md`. Diff coverage on added/changed code at the configured threshold (default ≥ 80%). The playbook is **language-aware**: it picks native coverage tooling per project (Go, JS/TS, Python, Rust, JVM, Ruby) and only uses Python's `diff-cover` when Python is already a project dependency. Do not pull Python into a non-Python repo.
3. **UI test gate** — `gates.ui_test` in `.ai-sdlc.yaml`. If the change touches UI, run the relevant browser/Playwright flows.
4. **Pre-commit gate** — `playbooks/pre-commit-gate.md`. Run `pre-commit run` against the changed files. Auto-skips (regardless of `enabled`) if `pre-commit` isn't installed or `.pre-commit-config.yaml` is missing.

When a gate is skipped (whether by config, manual mode, or auto-skip), record the **specific reason** in `05-qa.md`. A failure at any *enabled* gate halts advancement. Resolve, re-run, then proceed.

## Decision points

- Test fails: bug in the change, flaky test, or pre-existing issue surfaced? Triage before patching.
- Coverage gap on added code: add tests, or get explicit user approval to exclude with reason.
- Pre-commit hook fails with auto-fix: re-stage and re-run; auto-fixes become part of the diff and require tests to be re-run.
- Pre-commit hook fails without auto-fix: fix the underlying issue. Do **not** use `--no-verify` and do **not** add per-line exemptions without user approval.
- UI testing required but no browser tooling available: ask the user to install or grant access.

## Tool calls (if available)

- Project test runner.
- Coverage toolchain — see `tools/well-known-tools.md` and `playbooks/coverage-gate.md`.
- `pre-commit run` — see `playbooks/pre-commit-gate.md`.
- Playwright/Browser MCP for UI flows.

## When to ask the user

- Tests that should exist but don't — confirm whether to add them in scope or defer.
- New dev dependencies (coverage tool, `pre-commit`, etc.) before installing.
- Any pre-commit finding you'd like to exempt rather than fix.
- UI testing access (browser MCP, dev server credentials).

## Exit criteria

- All planned tests pass.
- Regression suite (or its agreed subset) passes.
- **Coverage gate** passed at the configured threshold, **or** skipped per `.ai-sdlc.yaml` (recorded with reason).
- **Pre-commit gate** clean on changed files, **or** skipped per `.ai-sdlc.yaml` / auto-skipped due to missing tooling (recorded with reason).
- **UI gate** passed if applicable, **or** skipped per `.ai-sdlc.yaml`.
- `05-qa.md` written, including the skip reason for each non-running gate.
- Manifest updated with `current_phase: 06-security`.
